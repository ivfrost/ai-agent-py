[project]
name = "ai-agent-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.109.1",
    "python-dotenv==1.1.0",
]

[dependency-groups]
dev = [
    "autopep8>=2.3.2",
]
